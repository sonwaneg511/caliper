spring.application.name=security
server.port=8080

management.endpoints.web.exposure.include=health,info,metrics
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=always

management.endpoint.env.access=none
management.endpoint.shutdown.access=none
management.endpoint.beans.access=none

management.endpoint.health.access=unrestricted
management.endpoint.info.access=unrestricted

info.app.name=Caliper MVP
info.app.version=1.0.0
info.app.description=Providing solution to multiple marketing needs on different Social Media Platform.

spring.security.user.password=demo@123
spring.security.user.name=admin

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.app.jwtSecret=mySecretKey123912738aopsgjnspkmndfsopkvajoirjg94gf2opfng2moknm
spring.app.jwtExpirationMs=3600000

logging.level.org.springframework=warn
logging.level.org.hibernate.SQL=warn
logging.level.org.springframework.security=warn
logging.level.org.springframework.boot.autoconfigure=ERROR

scheduler.logs.directory=./logs/jobs

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

spring.profiles.active=local

# Email / SMTP (credentials and reset URL are set per-environment in profile properties)
spring.mail.host=${SMTP_HOST:email-smtp.ap-south-1.amazonaws.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:placeholder}
spring.mail.password=${SMTP_PASSWORD:placeholder}
spring.mail.from=${SMTP_FROM:adtech@interactiveavenues.net}

# Razorpay Payment Gateway
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_FrS0vPNhqINoNZ}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:1knPuqRo5maEhFWgxv7FdVDb}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:placeholder}

# GST Configuration (intra-state: CGST + SGST)
gst.cgst.rate=9.0
gst.sgst.rate=9.0

# Campaign payment agency commission (applied before GST)
campaign.commission.rate=10.0

# Meta Ads Webhook
meta.webhook.verify.token=caliper_meta_verify_token

